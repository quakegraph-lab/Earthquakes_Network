# Earthquake Network Analysis — Python dependencies
# Python 3.11+ required (project developed on Python 3.14)
#
# Install:  pip install -r requirements.txt
# Activate: source venv_earthquakes/bin/activate

# ── Core scientific stack ─────────────────────────────────────────────────────
numpy>=2.0
pandas>=2.0
scipy>=1.12

# ── Network analysis ──────────────────────────────────────────────────────────
networkx>=3.3

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7          # 3.7+ required (colormaps[] API)
seaborn>=0.13
plotly>=5.18             # 5.18+ required (scatter_map / Scattermap API)
kaleido>=0.2             # static Plotly export (PDF/JPG)

# ── Geospatial ────────────────────────────────────────────────────────────────
geopandas>=1.0

# ── Machine learning / clustering ────────────────────────────────────────────
scikit-learn>=1.4

# ── Community detection ───────────────────────────────────────────────────────
infomap>=2.7             # InfoMap flow-based community detection
leidenalg>=0.10          # Leiden algorithm (directed Louvain)
igraph>=0.11             # python-igraph, required by leidenalg
hdbscan>=0.8.33          # HDBSCAN density-based community detection
karateclub>=1.3.3        # BigCLAM overlapping community detection

# ── Power-law fitting ─────────────────────────────────────────────────────────
powerlaw>=1.4

# ── Force-directed layout ─────────────────────────────────────────────────────
fa2-modified>=0.3        # ForceAtlas2 (pure Python, works on Python 3.11+)

# ── Notebook infrastructure ───────────────────────────────────────────────────
jupyterlab>=4.0
nbformat>=5.9
